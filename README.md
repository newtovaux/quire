# quire
Markdown notes kept as plain .md files on your own disk. One HTML file, no build, no database, no accounts. Pick a folder: subfolders become libraries, files become notes. Autosave, a writing toolbar, live checkboxes, fuzzy jump-to-note across every library, and safe moves. Chromium only, via the File System Access API.
